
//C++ includes
#include <iostream>
#include <string>
#include <fstream>
#include <vector>
#include <sstream>
using namespace std;

//My includes
#include "histograms.h"
using namespace Histograms;

class iterations{
 public:
  virtual void grab_iterations();

  

};

void iterations::grab_iterations(){

  cout<<"Start of Grabv Iterations"<<endl;

  ifstream file;
  file.open("num_iterations.csv");


  while(file.good()){
    string line;
    getline(file,line,',');
    cout<<line<< endl;
  }


  /*
  vector<vector<string>> content;
  vector<string> row;
  string line, word;


  while(getline(file, line)){
    row.clear();
    stringstream str(line);
    
    while(getline(str, word, ',')){
      row.push_back(word);
    }
    content.push_back(row);
  }

  cout<<"Size of Content "<<content.size()<<endl;

  for(int i=0; i < content.size();i++){
    cout<<"Value of i: "<<i<<endl;
    
    for(int j=0;j<content[i].size();j++){
      cout<<"value of j: "<<j<<endl;
      cout<<content[i][j]<<" "<<endl;
    }
    cout<<"\n";
  }
  */
cout<<"End of Grabv Iterations"<<endl;
  
} //end of grab_iterations
